node_modules/
.pnpm-store/

.turbo/
.next/
out/
dist/
build/
.deploy/

coverage/
playwright-report/
test-results/
e2e/.auth/
*.tsbuildinfo

.env
.env.local
.env.*.local
!.env.example

# A1 digital certificates — NEVER commit. The env cert ships as a base64
# secret; `pnpm gen:test-cert` writes throwaway .pfx files to test the upload.
*.pfx
*.p12
# Committed (non-secret): feeds the FUNCTIONS_REGION param to Firebase
# non-interactively (emulator + deploy). See apps/functions/src/options.ts.
!apps/functions/.env
.secrets/

.DS_Store
*.log
.idea/
.vscode/

.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

.old/
devlogs/

.claude/settings.local.json
.claude/worktrees/

*next-env.d.ts
*env.d.ts
.ignore/

# Locally-rendered DANFE samples for manual review (pnpm render:danfe-samples)
danfe-samples/
# local scratch (trace/error-context extraction)
tmp-e2e/

# local e2e debug artifact downloads
dbg_e2e_2/
dbg_trace/
