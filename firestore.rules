// GENERATED by @delfrance/rules-gen — DO NOT EDIT.
// Regenerate: pnpm --filter @delfrance/rules-gen gen:rules
//
// Deploy stays manual/coordinated (CLAUDE.md critical rule 1). PRODUCTION runs
// this file; the STAGING project runs the --e2e variant (firestore.e2e.rules
// via firebase.staging.json), which adds the e2e_<runId>_* namespace block so
// Playwright fixtures aren't default-denied. These fixed paths do not cover
// those namespaces — that is intentional for production.

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Claim bit test without bitwise operators (rules CEL has none):
    // d_* claims are small ints, k is 1 (read), 2 (write) or 4 (delete).
    function p(d, k) {
      return request.auth != null && (request.auth.token.get(d, 0) / k) % 2 == 1;
    }

    // Break-glass super user: the dedicated `su` claim short-circuits the
    // permission + tenancy checks below (field validators still apply). Minted
    // server-side only for usuario.isSuperUser accounts — never self-grantable.
    function isSuperUser() {
      return request.auth != null && request.auth.token.get('su', false) == true;
    }

    // d = incoming document, c = touched keys (keys() on create,
    // diff().affectedKeys() on update — legacy docs stay updatable).
    function v_clientes(d, c) {
      return (!c.hasAny(['cpf_cnpj']) || (d.get('cpf_cnpj', null) == null || d.get('cpf_cnpj', null) is string && d.get('cpf_cnpj', null).size() <= 18))
        && (!c.hasAny(['email']) || (d.get('email', null) == null || d.get('email', null) is string && d.get('email', null).size() <= 255))
        && (!c.hasAny(['idEstrangeiro']) || (d.get('idEstrangeiro', null) == null || d.get('idEstrangeiro', null) is string && d.get('idEstrangeiro', null).size() <= 20))
        && (!c.hasAny(['ie']) || (d.get('ie', null) == null || d.get('ie', null) is string && d.get('ie', null).size() <= 16))
        && (!c.hasAny(['imun']) || (d.get('imun', null) == null || d.get('imun', null) is string && d.get('imun', null).size() <= 15))
        && (!c.hasAny(['isUF']) || (d.get('isUF', null) == null || d.get('isUF', null) is string && d.get('isUF', null).size() <= 9))
        && (!c.hasAny(['nome']) || (d.get('nome', null) == null || d.get('nome', null) is string && d.get('nome', null).size() <= 255))
        && (!c.hasAny(['observacoesInternas']) || (d.get('observacoesInternas', null) == null || d.get('observacoesInternas', null) is string && d.get('observacoesInternas', null).size() <= 255))
        && (!c.hasAny(['telefone']) || (d.get('telefone', null) == null || d.get('telefone', null) is string && d.get('telefone', null).size() <= 16))
        && (!c.hasAny(['timestamp']) || (d.get('timestamp', null) == null || d.get('timestamp', null) is int))
        && (!c.hasAny(['tipo']) || (d.get('tipo', null) == null || d.get('tipo', null) in ['0', '1', '2']))
        && (!c.hasAny(['ultimaModificacao']) || (d.get('ultimaModificacao', null) == null || d.get('ultimaModificacao', null) is int))
        && (!c.hasAny(['userCliente']) || (d.get('userCliente', null) == null || d.get('userCliente', null) is string));
    }

    // d = incoming document, c = touched keys (keys() on create,
    // diff().affectedKeys() on update — legacy docs stay updatable).
    function v_metodo_pgto(d, c) {
      return (!c.hasAny(['dataCadastro']) || (d.get('dataCadastro', null) == null || d.get('dataCadastro', null) is int))
        && (!c.hasAny(['hasLinkPagamento']) || d.get('hasLinkPagamento', null) is bool)
        && (!c.hasAny(['nome']) || d.get('nome', null) is string && d.get('nome', null).size() <= 255)
        && (!c.hasAny(['tipo']) || d.get('tipo', null) == 1)
        && (!c.hasAny(['ultimaModificacao']) || (d.get('ultimaModificacao', null) == null || d.get('ultimaModificacao', null) is int))
        && (!c.hasAny(['user_id']) || (d.get('user_id', null) == null || d.get('user_id', null) is int));
    }

    // d = incoming document, c = touched keys (keys() on create,
    // diff().affectedKeys() on update — legacy docs stay updatable).
    function v_pedidos(d, c) {
      return (!c.hasAny(['bloquearEmissaoNFe']) || (d.get('bloquearEmissaoNFe', null) == null || d.get('bloquearEmissaoNFe', null) is bool))
        && (!c.hasAny(['chNFeReferenciadas']) || (d.get('chNFeReferenciadas', null) == null || d.get('chNFeReferenciadas', null) is list))
        && (!c.hasAny(['clientePedidoOuterRef']) || (d.get('clientePedidoOuterRef', null) == null || d.get('clientePedidoOuterRef', null) is string))
        && (!c.hasAny(['dataFinalExpedicao']) || (d.get('dataFinalExpedicao', null) == null || d.get('dataFinalExpedicao', null) is int))
        && (!c.hasAny(['dataIndisponivelEstoque']) || (d.get('dataIndisponivelEstoque', null) == null || d.get('dataIndisponivelEstoque', null) is int))
        && (!c.hasAny(['dataRemocaoEstoque']) || (d.get('dataRemocaoEstoque', null) == null || d.get('dataRemocaoEstoque', null) is int))
        && (!c.hasAny(['descontoTotal']) || d.get('descontoTotal', null) is number)
        && (!c.hasAny(['dtImpressao']) || (d.get('dtImpressao', null) == null || d.get('dtImpressao', null) is int))
        && (!c.hasAny(['ehSaida']) || d.get('ehSaida', null) is bool)
        && (!c.hasAny(['enderecoFiscalOuterRef']) || (d.get('enderecoFiscalOuterRef', null) == null || d.get('enderecoFiscalOuterRef', null) is string))
        && (!c.hasAny(['entradasRelacionadas']) || (d.get('entradasRelacionadas', null) == null || d.get('entradasRelacionadas', null) is list))
        && (!c.hasAny(['error']) || (d.get('error', null) == null || d.get('error', null) is string))
        && (!c.hasAny(['estado']) || d.get('estado', null) in ['iniciado', 'carrinho', 'carrinhoAbandonado', 'escolhendoFormaDePagamento', 'aguardandoConfirmacaoDePagamento', 'pagamentoNaoRealizado', 'emAnalise', 'emProcessamento', 'pago', 'estornadoParcialmente', 'estornadoIntegralmente', 'processandoCancelamento', 'cancelado', 'fraude', 'finalizado', 'error'])
        && (!c.hasAny(['estoqueAplicado']) || (d.get('estoqueAplicado', null) == null || d.get('estoqueAplicado', null) is map))
        && (!c.hasAny(['foiImpresso']) || d.get('foiImpresso', null) is bool)
        && (!c.hasAny(['freteInicial']) || (d.get('freteInicial', null) == null || d.get('freteInicial', null) is map))
        && (!c.hasAny(['hasUserInteraction']) || (d.get('hasUserInteraction', null) == null || d.get('hasUserInteraction', null) is bool))
        && (!c.hasAny(['impostos']) || (d.get('impostos', null) == null || d.get('impostos', null) is number))
        && (!c.hasAny(['infCpl']) || (d.get('infCpl', null) == null || d.get('infCpl', null) is string))
        && (!c.hasAny(['integracaoPedidoOuterRef']) || (d.get('integracaoPedidoOuterRef', null) == null || d.get('integracaoPedidoOuterRef', null) is string))
        && (!c.hasAny(['itens']) || d.get('itens', null) is map)
        && (!c.hasAny(['itensDevolvidos']) || (d.get('itensDevolvidos', null) == null || d.get('itensDevolvidos', null) is map))
        && (!c.hasAny(['itensIds']) || d.get('itensIds', null) is list)
        && (!c.hasAny(['lastMarketplaceUpdate']) || (d.get('lastMarketplaceUpdate', null) == null || d.get('lastMarketplaceUpdate', null) is int))
        && (!c.hasAny(['listaDePrecosOuterRef']) || (d.get('listaDePrecosOuterRef', null) == null || d.get('listaDePrecosOuterRef', null) is string))
        && (!c.hasAny(['numero']) || (d.get('numero', null) == null || d.get('numero', null) is string))
        && (!c.hasAny(['observacoesInternas']) || (d.get('observacoesInternas', null) == null || d.get('observacoesInternas', null) is string))
        && (!c.hasAny(['operacaoPedidoOuterRef']) || (d.get('operacaoPedidoOuterRef', null) == null || d.get('operacaoPedidoOuterRef', null) is string))
        && (!c.hasAny(['saidasRelacionadas']) || (d.get('saidasRelacionadas', null) == null || d.get('saidasRelacionadas', null) is list))
        && (!c.hasAny(['timestamp']) || (d.get('timestamp', null) == null || d.get('timestamp', null) is int))
        && (!c.hasAny(['ultimaModificacao']) || (d.get('ultimaModificacao', null) == null || d.get('ultimaModificacao', null) is int))
        && (!c.hasAny(['valorCobrado']) || (d.get('valorCobrado', null) == null || d.get('valorCobrado', null) is number))
        && (!c.hasAny(['valorComissoes']) || (d.get('valorComissoes', null) == null || d.get('valorComissoes', null) is number))
        && (!c.hasAny(['valorDespesasIncidentes']) || (d.get('valorDespesasIncidentes', null) == null || d.get('valorDespesasIncidentes', null) is number))
        && (!c.hasAny(['valorFretesIncidentes']) || (d.get('valorFretesIncidentes', null) == null || d.get('valorFretesIncidentes', null) is number))
        && (!c.hasAny(['vendedorPedidoOuterRef']) || (d.get('vendedorPedidoOuterRef', null) == null || d.get('vendedorPedidoOuterRef', null) is string));
    }

    // d = incoming document, c = touched keys (keys() on create,
    // diff().affectedKeys() on update — legacy docs stay updatable).
    function v_pedidos_pagamentos(d, c) {
      return (!c.hasAny(['aVista']) || d.get('aVista', null) is bool)
        && (!c.hasAny(['dataAprovacao']) || (d.get('dataAprovacao', null) == null || d.get('dataAprovacao', null) is int))
        && (!c.hasAny(['dataCadastro']) || (d.get('dataCadastro', null) == null || d.get('dataCadastro', null) is int))
        && (!c.hasAny(['dataCancelamento']) || (d.get('dataCancelamento', null) == null || d.get('dataCancelamento', null) is int))
        && (!c.hasAny(['descricaoPagamento']) || (d.get('descricaoPagamento', null) == null || d.get('descricaoPagamento', null) is string))
        && (!c.hasAny(['duplicata']) || d.get('duplicata', null) is bool)
        && (!c.hasAny(['id']) || (d.get('id', null) == null || d.get('id', null) is string))
        && (!c.hasAny(['juros']) || (d.get('juros', null) == null || (d.get('juros', null) is number && d.get('juros', null) >= 0)))
        && (!c.hasAny(['metodoPagamentoOuterRef']) || (d.get('metodoPagamentoOuterRef', null) == null || d.get('metodoPagamentoOuterRef', null) is string))
        && (!c.hasAny(['nFat']) || (d.get('nFat', null) == null || d.get('nFat', null) is string && d.get('nFat', null).size() <= 60))
        && (!c.hasAny(['parcelas']) || (d.get('parcelas', null) is int && d.get('parcelas', null) >= 1))
        && (!c.hasAny(['tarifas']) || (d.get('tarifas', null) == null || (d.get('tarifas', null) is number && d.get('tarifas', null) >= 0)))
        && (!c.hasAny(['ultimaModificacao']) || (d.get('ultimaModificacao', null) == null || d.get('ultimaModificacao', null) is int))
        && (!c.hasAny(['valor']) || (d.get('valor', null) is number && d.get('valor', null) >= 0))
        && (!c.hasAny(['vencimento']) || (d.get('vencimento', null) == null || d.get('vencimento', null) is int));
    }

    // d = incoming document, c = touched keys (keys() on create,
    // diff().affectedKeys() on update — legacy docs stay updatable).
    function v_produtos(d, c) {
      return (!c.hasAny(['alturaCm']) || (d.get('alturaCm', null) == null || d.get('alturaCm', null) is number))
        && (!c.hasAny(['anexos']) || (d.get('anexos', null) == null || d.get('anexos', null) is list))
        && (!c.hasAny(['categoriaProdutoOuterRef']) || (d.get('categoriaProdutoOuterRef', null) == null || d.get('categoriaProdutoOuterRef', null) is string))
        && (!c.hasAny(['codFornecedor']) || (d.get('codFornecedor', null) == null || d.get('codFornecedor', null) is string && d.get('codFornecedor', null).size() <= 255))
        && (!c.hasAny(['codPai']) || (d.get('codPai', null) == null || d.get('codPai', null) is string && d.get('codPai', null).size() <= 255))
        && (!c.hasAny(['componentesKit']) || (d.get('componentesKit', null) == null || d.get('componentesKit', null) is map))
        && (!c.hasAny(['componentesKitKeys']) || (d.get('componentesKitKeys', null) == null || d.get('componentesKitKeys', null) is list))
        && (!c.hasAny(['crossdocking']) || (d.get('crossdocking', null) == null || (d.get('crossdocking', null) is int && d.get('crossdocking', null) >= 0)))
        && (!c.hasAny(['custo']) || (d.get('custo', null) == null || (d.get('custo', null) is number && d.get('custo', null) >= 0)))
        && (!c.hasAny(['ehKit']) || d.get('ehKit', null) is bool)
        && (!c.hasAny(['ehKitVirtual']) || d.get('ehKitVirtual', null) is bool)
        && (!c.hasAny(['ehUsado']) || d.get('ehUsado', null) is bool)
        && (!c.hasAny(['fotos']) || (d.get('fotos', null) == null || d.get('fotos', null) is list))
        && (!c.hasAny(['fotosArquivosIds']) || (d.get('fotosArquivosIds', null) == null || d.get('fotosArquivosIds', null) is list))
        && (!c.hasAny(['grupoDeVariacoesUid']) || (d.get('grupoDeVariacoesUid', null) == null || d.get('grupoDeVariacoesUid', null) is list))
        && (!c.hasAny(['gtin']) || (d.get('gtin', null) == null || d.get('gtin', null) is string && d.get('gtin', null).size() <= 255))
        && (!c.hasAny(['integracoesComProduto']) || d.get('integracoesComProduto', null) is list)
        && (!c.hasAny(['larguraCm']) || (d.get('larguraCm', null) == null || d.get('larguraCm', null) is number))
        && (!c.hasAny(['marketplace']) || d.get('marketplace', null) is list)
        && (!c.hasAny(['marketplaceIds']) || (d.get('marketplaceIds', null) == null || d.get('marketplaceIds', null) is list))
        && (!c.hasAny(['nome']) || d.get('nome', null) is string && d.get('nome', null).size() <= 100)
        && (!c.hasAny(['ofereceFreteGratis']) || d.get('ofereceFreteGratis', null) is bool)
        && (!c.hasAny(['ordem']) || (d.get('ordem', null) == null || d.get('ordem', null) is int))
        && (!c.hasAny(['paiId']) || (d.get('paiId', null) == null || d.get('paiId', null) is string))
        && (!c.hasAny(['permiteVendaSemEstoque']) || d.get('permiteVendaSemEstoque', null) is bool)
        && (!c.hasAny(['pesoBrutoKg']) || (d.get('pesoBrutoKg', null) == null || d.get('pesoBrutoKg', null) is number))
        && (!c.hasAny(['pesoLiquidoKg']) || (d.get('pesoLiquidoKg', null) == null || d.get('pesoLiquidoKg', null) is number))
        && (!c.hasAny(['precos']) || (d.get('precos', null) == null || d.get('precos', null) is map))
        && (!c.hasAny(['profundidadeCm']) || (d.get('profundidadeCm', null) == null || d.get('profundidadeCm', null) is number))
        && (!c.hasAny(['propagatePriceToChildren']) || d.get('propagatePriceToChildren', null) is bool)
        && (!c.hasAny(['publicado']) || d.get('publicado', null) is bool)
        && (!c.hasAny(['sku']) || (d.get('sku', null) == null || d.get('sku', null) is string && d.get('sku', null).size() <= 255))
        && (!c.hasAny(['statusProdutosMarketplace']) || (d.get('statusProdutosMarketplace', null) == null || d.get('statusProdutosMarketplace', null) is map))
        && (!c.hasAny(['tabelaDeMedidasModaUid']) || (d.get('tabelaDeMedidasModaUid', null) == null || d.get('tabelaDeMedidasModaUid', null) is string))
        && (!c.hasAny(['timestamp']) || (d.get('timestamp', null) == null || d.get('timestamp', null) is int))
        && (!c.hasAny(['ultimaModificacao']) || (d.get('ultimaModificacao', null) == null || d.get('ultimaModificacao', null) is int))
        && (!c.hasAny(['variacoesUid']) || (d.get('variacoesUid', null) == null || d.get('variacoesUid', null) is list))
        && (!c.hasAny(['videos']) || (d.get('videos', null) == null || d.get('videos', null) is list));
    }

    match /arquivos/{docId} {
      allow read: if isSuperUser() || p('d_arquivo', 1);
      allow create, update: if isSuperUser() || p('d_arquivo', 2);
      allow delete: if isSuperUser() || p('d_arquivo', 4);
    }

    match /balanco/{docId} {
      allow read: if isSuperUser() || p('d_estoque', 1);
      allow create: if (isSuperUser() || p('d_estoque', 2)) && (!request.resource.data.keys().hasAny(['dataFinalizado']) || request.resource.data.get('dataFinalizado', null) == null) && (!request.resource.data.keys().hasAny(['estado']) || request.resource.data.get('estado', null) == null) && (!request.resource.data.keys().hasAny(['finalizacao']) || request.resource.data.get('finalizacao', null) == null);
      allow update: if (isSuperUser() || p('d_estoque', 2)) && !request.resource.data.diff(resource.data).affectedKeys().hasAny(['dataFinalizado', 'estado', 'finalizacao']);
      allow delete: if isSuperUser() || p('d_estoque', 4);
    }

    match /balanco/{balancoId}/movimentos/{docId} {
      allow read: if isSuperUser() || p('d_estoque', 1);
      allow create, update: if isSuperUser() || p('d_estoque', 2);
      allow delete: if isSuperUser() || p('d_estoque', 4);
    }

    match /balanco/{balancoId}/relatorios/{docId} {
      allow read: if isSuperUser() || p('d_estoque', 1);
      // Server-owned collection — Admin SDK only, no su bypass.
      allow create, update, delete: if false;
    }

    match /bandeirasCartao/{docId} {
      allow read: if isSuperUser() || p('d_pagamento', 1);
      allow create, update: if isSuperUser() || p('d_pagamento', 2);
      allow delete: if isSuperUser() || p('d_pagamento', 4);
    }

    match /cargos/{docId} {
      allow read: if isSuperUser() || p('d_configuracoes', 1);
      allow create, update: if isSuperUser() || p('d_configuracoes', 2);
      allow delete: if isSuperUser() || p('d_configuracoes', 2);
    }

    match /categorias/{docId} {
      allow read: if isSuperUser() || p('d_categoria', 1);
      allow create, update: if isSuperUser() || p('d_categoria', 2);
      allow delete: if isSuperUser() || p('d_categoria', 4);
    }

    match /categorias/{categoriaId}/imposto/{docId} {
      allow read: if isSuperUser() || p('d_impostoCategoria', 1);
      allow create, update: if isSuperUser() || p('d_impostoCategoria', 2);
      allow delete: if isSuperUser() || p('d_impostoCategoria', 4);
    }

    match /chat/{docId} {
      allow read: if isSuperUser() || p('d_chat', 1);
      allow create, update: if isSuperUser() || p('d_chat', 2);
      allow delete: if isSuperUser() || p('d_chat', 4);
    }

    match /chat/{conversaId}/mensagem/{docId} {
      allow read: if isSuperUser() || p('d_mensagem', 1);
      allow create, update: if isSuperUser() || p('d_mensagem', 2);
      allow delete: if isSuperUser() || p('d_mensagem', 4);
    }

    match /clientes/{docId} {
      allow read: if isSuperUser() || p('d_cliente', 1);
      allow create: if (isSuperUser() || p('d_cliente', 2)) && v_clientes(request.resource.data, request.resource.data.keys());
      allow update: if (isSuperUser() || p('d_cliente', 2)) && v_clientes(request.resource.data, request.resource.data.diff(resource.data).affectedKeys());
      allow delete: if isSuperUser() || p('d_cliente', 4);
    }

    match /clientes/{clienteId}/enderecos/{docId} {
      allow read: if isSuperUser() || p('d_endereco', 1);
      allow create, update: if isSuperUser() || p('d_endereco', 2);
      allow delete: if isSuperUser() || p('d_endereco', 4);
    }

    match /CMUN/{docId} {
      allow read: if isSuperUser() || p('d_cmun', 1);
      // Server-owned collection — Admin SDK only, no su bypass.
      allow create, update, delete: if false;
    }

    match /configIa/{docId} {
      allow read: if isSuperUser() || p('d_integracao', 1);
      allow create, update: if isSuperUser() || p('d_integracao', 2);
      allow delete: if isSuperUser() || p('d_integracao', 4);
    }

    match /counters/{docId} {
      allow read: if isSuperUser() || p('d_pedido', 1);
      allow create, update: if isSuperUser() || p('d_pedido', 2);
      allow delete: if isSuperUser() || p('d_pedido', 4);
    }

    match /depositos/{docId} {
      allow read: if isSuperUser() || p('d_estoque', 1);
      allow create, update: if isSuperUser() || p('d_estoque', 2);
      allow delete: if isSuperUser() || p('d_estoque', 4);
    }

    match /filiais/{docId} {
      allow read: if isSuperUser() || p('d_configuracoes', 1);
      allow create, update: if isSuperUser() || p('d_configuracoes', 2);
      allow delete: if isSuperUser() || p('d_configuracoes', 2);
    }

    match /filiais/{filialId}/enviNfe/{docId} {
      allow read: if isSuperUser() || p('d_fiscal', 1);
      allow create, update: if isSuperUser() || p('d_fiscal', 2);
      allow delete: if isSuperUser() || p('d_fiscal', 4);
    }

    match /filiais/{filialId}/inutilizacao/{docId} {
      allow read: if isSuperUser() || p('d_fiscal', 1);
      allow create, update: if isSuperUser() || p('d_fiscal', 2);
      allow delete: if isSuperUser() || p('d_fiscal', 4);
    }

    match /filiais/{filialId}/nfeconfig/{docId} {
      allow read: if isSuperUser() || p('d_fiscal', 1);
      allow create, update: if isSuperUser() || p('d_fiscal', 2);
      allow delete: if isSuperUser() || p('d_fiscal', 4);
    }

    match /grupoDeVariacoes/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create, update: if isSuperUser() || p('d_produto', 2);
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /int_frete/{docId} {
      allow read: if isSuperUser() || p('d_frete', 1);
      allow create, update: if isSuperUser() || p('d_frete', 2);
      allow delete: if isSuperUser() || p('d_frete', 4);
    }

    match /int_frete/{intFreteId}/tokenMelEnv/{docId} {
      allow read: if isSuperUser() || p('d_frete', 2);
      allow create, update: if isSuperUser() || p('d_frete', 2);
      allow delete: if isSuperUser() || p('d_frete', 4);
    }

    match /integracao/{docId} {
      allow read: if isSuperUser() || p('d_integracao', 1);
      allow create: if (isSuperUser() || p('d_integracao', 2)) && (!request.resource.data.keys().hasAny(['user_id']) || request.resource.data.get('user_id', null) == null);
      allow update: if (isSuperUser() || p('d_integracao', 2)) && !request.resource.data.diff(resource.data).affectedKeys().hasAny(['user_id']);
      allow delete: if isSuperUser() || p('d_integracao', 4);
    }

    match /integracao/{integracaoId}/brandshopee/{docId} {
      allow read: if isSuperUser() || p('d_integracao', 1);
      allow create, update: if isSuperUser() || p('d_integracao', 2);
      allow delete: if isSuperUser() || p('d_integracao', 4);
    }

    match /integracao/{integracaoId}/token6h/{docId} {
      allow read: if isSuperUser() || p('d_integracao', 1);
      allow create, update: if isSuperUser() || p('d_integracao', 2);
      allow delete: if isSuperUser() || p('d_integracao', 4);
    }

    match /integracao/{integracaoId}/tokenDuravel/{docId} {
      allow read: if isSuperUser() || p('d_integracao', 1);
      allow create, update: if isSuperUser() || p('d_integracao', 2);
      allow delete: if isSuperUser() || p('d_integracao', 4);
    }

    match /listaDePrecos/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create, update: if isSuperUser() || p('d_produto', 2);
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /metodo_pgto/{docId} {
      allow read: if isSuperUser() || p('d_metodoPagamento', 1);
      allow create: if (isSuperUser() || p('d_metodoPagamento', 2)) && v_metodo_pgto(request.resource.data, request.resource.data.keys()) && (!request.resource.data.keys().hasAny(['user_id']) || request.resource.data.get('user_id', null) == null);
      allow update: if (isSuperUser() || p('d_metodoPagamento', 2)) && v_metodo_pgto(request.resource.data, request.resource.data.diff(resource.data).affectedKeys()) && !request.resource.data.diff(resource.data).affectedKeys().hasAny(['user_id']);
      allow delete: if isSuperUser() || p('d_metodoPagamento', 4);
    }

    match /motivosincidentes/{docId} {
      allow read: if isSuperUser() || p('d_pedido', 1);
      allow create, update: if isSuperUser() || p('d_pedido', 2);
      allow delete: if isSuperUser() || p('d_pedido', 4);
    }

    match /notificacoesMercadoLivre/{docId} {
      allow read: if isSuperUser() || p('d_integracao', 1);
      allow create, update: if isSuperUser() || p('d_integracao', 2);
      allow delete: if isSuperUser() || p('d_integracao', 4);
    }

    match /operacao/{docId} {
      allow read: if isSuperUser() || p('d_fiscal', 1);
      allow create, update: if isSuperUser() || p('d_fiscal', 2);
      allow delete: if isSuperUser() || p('d_fiscal', 4);
    }

    match /operacao/{operacaoId}/regras/{docId} {
      allow read: if isSuperUser() || p('d_regraImposto', 1);
      allow create, update: if isSuperUser() || p('d_regraImposto', 2);
      allow delete: if isSuperUser() || p('d_regraImposto', 4);
    }

    match /pedidos/{docId} {
      allow read: if isSuperUser() || p('d_pedido', 1);
      allow create: if (isSuperUser() || p('d_pedido', 2)) && v_pedidos(request.resource.data, request.resource.data.keys()) && (!request.resource.data.keys().hasAny(['estoqueAplicado']) || request.resource.data.get('estoqueAplicado', null) == null);
      allow update: if (isSuperUser() || p('d_pedido', 2)) && v_pedidos(request.resource.data, request.resource.data.diff(resource.data).affectedKeys()) && !request.resource.data.diff(resource.data).affectedKeys().hasAny(['estoqueAplicado']);
      allow delete: if isSuperUser() || p('d_pedido', 4);
    }

    match /pedidos/{pedidoId}/checkout/{docId} {
      allow read: if isSuperUser() || p('d_pedido', 1);
      allow create, update: if isSuperUser() || p('d_pedido', 2);
      allow delete: if isSuperUser() || p('d_pedido', 4);
    }

    match /pedidos/{pedidoId}/historicoEstadoPedido/{docId} {
      allow read: if isSuperUser() || p('d_pedido', 1);
      // Server-owned collection — Admin SDK only, no su bypass.
      allow create, update, delete: if false;
    }

    match /pedidos/{pedidoId}/historicoFtIni/{docId} {
      allow read: if isSuperUser() || p('d_pedido', 1);
      // Server-owned collection — Admin SDK only, no su bypass.
      allow create, update, delete: if false;
    }

    match /pedidos/{pedidoId}/incidentes/{docId} {
      allow read: if isSuperUser() || p('d_pedido', 1);
      allow create, update: if isSuperUser() || p('d_pedido', 2);
      allow delete: if isSuperUser() || p('d_pedido', 4);
    }

    match /pedidos/{pedidoId}/nfev4/{docId} {
      allow read: if isSuperUser() || p('d_nfe', 1);
      allow create, update: if isSuperUser() || p('d_nfe', 2);
      allow delete: if isSuperUser() || p('d_nfe', 4);
    }

    match /pedidos/{pedidoId}/nfev4/{nfeId}/cartacorrecao/{docId} {
      allow read: if isSuperUser() || p('d_fiscal', 1);
      allow create, update: if isSuperUser() || p('d_fiscal', 2);
      allow delete: if isSuperUser() || p('d_fiscal', 4);
    }

    match /pedidos/{pedidoId}/orderML/{docId} {
      allow read: if isSuperUser() || p('d_pedido', 1);
      allow create, update: if isSuperUser() || p('d_pedido', 2);
      allow delete: if isSuperUser() || p('d_pedido', 4);
    }

    match /pedidos/{pedidoId}/pagamentos/{docId} {
      allow read: if isSuperUser() || p('d_pagamento', 1);
      allow create: if (isSuperUser() || p('d_pagamento', 2)) && v_pedidos_pagamentos(request.resource.data, request.resource.data.keys());
      allow update: if (isSuperUser() || p('d_pagamento', 2)) && v_pedidos_pagamentos(request.resource.data, request.resource.data.diff(resource.data).affectedKeys());
      allow delete: if isSuperUser() || p('d_pagamento', 4);
    }

    match /produtos/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create: if (isSuperUser() || p('d_produto', 2)) && v_produtos(request.resource.data, request.resource.data.keys());
      allow update: if (isSuperUser() || p('d_produto', 2)) && v_produtos(request.resource.data, request.resource.data.diff(resource.data).affectedKeys());
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /produtos/{produtoId}/estoques/{docId} {
      allow read: if isSuperUser() || p('d_estoque', 1);
      allow create, update: if isSuperUser() || p('d_estoque', 2);
      allow delete: if isSuperUser() || p('d_estoque', 4);
    }

    match /produtos/{produtoId}/estoques/{estoqueId}/historicoEstoque/{docId} {
      allow read: if isSuperUser() || p('d_estoque', 1);
      allow create, update: if isSuperUser() || p('d_estoque', 2);
      allow delete: if isSuperUser() || p('d_estoque', 4);
    }

    match /produtos/{produtoId}/extraData/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create, update: if isSuperUser() || p('d_produto', 2);
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /produtos/{produtoId}/historicoDeCusto/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create, update: if isSuperUser() || p('d_produto', 2);
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /produtos/{produtoId}/historicoDeModificacoes/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      // Server-owned collection — Admin SDK only, no su bypass.
      allow create, update, delete: if false;
    }

    match /produtos/{produtoId}/historicoDePrecos/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create, update: if isSuperUser() || p('d_produto', 2);
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /produtos/{produtoId}/imposto/{docId} {
      allow read: if isSuperUser() || p('d_impostoProduto', 1);
      allow create, update: if isSuperUser() || p('d_impostoProduto', 2);
      allow delete: if isSuperUser() || p('d_impostoProduto', 4);
    }

    match /produtos/{produtoId}/prodAmazon/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create, update: if isSuperUser() || p('d_produto', 2);
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /produtos/{produtoId}/prodshopee/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create, update: if isSuperUser() || p('d_produto', 2);
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /produtos/{produtoId}/produtolojaintegrada/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create, update: if isSuperUser() || p('d_produto', 2);
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /produtos/{produtoId}/produtoMagalu2/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create, update: if isSuperUser() || p('d_produto', 2);
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /produtos/{produtoId}/produtoMercadoLivre/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create, update: if isSuperUser() || p('d_produto', 2);
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /produtos/{produtoId}/variacaoMercadoLivre/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create, update: if isSuperUser() || p('d_produto', 2);
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /produtos/{produtoId}/variashopee/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create, update: if isSuperUser() || p('d_produto', 2);
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /questionsML/{docId} {
      allow read: if isSuperUser() || p('d_integracao', 1);
      allow create, update: if isSuperUser() || p('d_integracao', 2);
      allow delete: if isSuperUser() || p('d_integracao', 4);
    }

    match /tabMedi/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
      allow create, update: if isSuperUser() || p('d_produto', 2);
      allow delete: if isSuperUser() || p('d_produto', 4);
    }

    match /usuarios/{docId} {
      allow read: if isSuperUser() || p('d_configuracoes', 1);
      allow create, update: if isSuperUser() || p('d_configuracoes', 2);
      allow delete: if isSuperUser() || p('d_configuracoes', 2);
    }

    match /grupoEconomico/{grupoId} {
      allow read: if isSuperUser() || (request.auth != null && request.auth.token.get('grupoEconomico', '') == grupoId);
    }

    // Collection-group reads (client groupQuery / collectionGroup).
    // A leaf shared by several collections carries the UNION of their
    // read claims (see the dedupe note in emitRules).
    match /{path=**}/brandshopee/{docId} {
      allow read: if isSuperUser() || p('d_integracao', 1);
    }
    match /{path=**}/cartacorrecao/{docId} {
      allow read: if isSuperUser() || p('d_fiscal', 1);
    }
    match /{path=**}/checkout/{docId} {
      allow read: if isSuperUser() || p('d_pedido', 1);
    }
    match /{path=**}/enderecos/{docId} {
      allow read: if isSuperUser() || p('d_endereco', 1);
    }
    match /{path=**}/enviNfe/{docId} {
      allow read: if isSuperUser() || p('d_fiscal', 1);
    }
    match /{path=**}/estoques/{docId} {
      allow read: if isSuperUser() || p('d_estoque', 1);
    }
    match /{path=**}/extraData/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
    }
    match /{path=**}/historicoDeCusto/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
    }
    match /{path=**}/historicoDeModificacoes/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
    }
    match /{path=**}/historicoDePrecos/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
    }
    match /{path=**}/historicoEstadoPedido/{docId} {
      allow read: if isSuperUser() || p('d_pedido', 1);
    }
    match /{path=**}/historicoEstoque/{docId} {
      allow read: if isSuperUser() || p('d_estoque', 1);
    }
    match /{path=**}/historicoFtIni/{docId} {
      allow read: if isSuperUser() || p('d_pedido', 1);
    }
    match /{path=**}/imposto/{docId} {
      allow read: if isSuperUser() || p('d_impostoCategoria', 1) || p('d_impostoProduto', 1);
    }
    match /{path=**}/incidentes/{docId} {
      allow read: if isSuperUser() || p('d_pedido', 1);
    }
    match /{path=**}/inutilizacao/{docId} {
      allow read: if isSuperUser() || p('d_fiscal', 1);
    }
    match /{path=**}/mensagem/{docId} {
      allow read: if isSuperUser() || p('d_mensagem', 1);
    }
    match /{path=**}/nfeconfig/{docId} {
      allow read: if isSuperUser() || p('d_fiscal', 1);
    }
    match /{path=**}/nfev4/{docId} {
      allow read: if isSuperUser() || p('d_nfe', 1);
    }
    match /{path=**}/orderML/{docId} {
      allow read: if isSuperUser() || p('d_pedido', 1);
    }
    match /{path=**}/pagamentos/{docId} {
      allow read: if isSuperUser() || p('d_pagamento', 1);
    }
    match /{path=**}/prodAmazon/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
    }
    match /{path=**}/prodshopee/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
    }
    match /{path=**}/produtoMagalu2/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
    }
    match /{path=**}/produtoMercadoLivre/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
    }
    match /{path=**}/produtolojaintegrada/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
    }
    match /{path=**}/regras/{docId} {
      allow read: if isSuperUser() || p('d_regraImposto', 1);
    }
    match /{path=**}/token6h/{docId} {
      allow read: if isSuperUser() || p('d_integracao', 1);
    }
    match /{path=**}/tokenDuravel/{docId} {
      allow read: if isSuperUser() || p('d_integracao', 1);
    }
    match /{path=**}/tokenMelEnv/{docId} {
      allow read: if isSuperUser() || p('d_frete', 2);
    }
    match /{path=**}/variacaoMercadoLivre/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
    }
    match /{path=**}/variashopee/{docId} {
      allow read: if isSuperUser() || p('d_produto', 1);
    }
  }
}
