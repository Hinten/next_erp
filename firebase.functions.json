{
  "$schema": "https://json.schemastore.org/firebase",
  "//": "Deploy-ISOLATED config used only by the storage emulator suite (ci-storage.yml: `firebase emulators:exec --config firebase.functions.json`). Kept separate from the root firebase.json so a stray `firebase deploy` can never push storage.rules over the Flutter-owned live rules, nor deploy the functions before that phase is coordinated (root `CLAUDE.md`, Critical rules).",
  "functions": [
    {
      "source": "apps/functions",
      "codebase": "storage",
      "runtime": "nodejs20",
      "ignore": [
        "node_modules",
        ".turbo",
        "src/**/*.test.ts",
        "src/**/*.storage.test.ts",
        "vitest*.ts"
      ]
    }
  ],
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules"
  },
  "emulators": {
    "auth": { "port": 9099 },
    "firestore": { "port": 8080 },
    "storage": { "port": 9199 },
    "functions": { "port": 5001 },
    "ui": { "enabled": false },
    "singleProjectMode": true
  }
}
