# Build output
**/.next/
**/dist/
**/out/
**/build/
**/coverage/
**/.turbo/
**/*.tsbuildinfo
**/playwright-report/
**/test-results/

# Dependencies & lockfile
**/node_modules/
pnpm-lock.yaml

# Codegen output (regenerated, never hand-formatted)
**/generated/

# Generated Firestore rulesets + their byte-for-byte snapshots.
# Emitted RAW by @delfrance/rules-gen (packages/rules-gen) and drift-checked
# byte-for-byte by ci-rules.yml — Prettier must never reflow them.
# (Today they're only spared because Prettier has no .rules/.snap parser;
# this makes the guarantee explicit so a future parser/plugin can't break CI.)
firestore.rules
firestore.e2e.rules
packages/rules-gen/src/__snapshots__/

# Prose docs & agent instructions — excluded to avoid markdown reflow churn.
# (Code, config, JSON/YAML and the root README are still formatted.)
**/CLAUDE.md
.claude/
.github/
apps/docs/
.changeset/

# Captured Mercado Livre wire bodies (#1087). These are ARTEFACTS, not source:
# `promote:fixtures` writes them with `JSON.stringify(v, null, 2)` and regenerates
# SHAPES.txt from exactly those bytes. Letting prettier reformat them makes the
# generator and the formatter fight — every promote produces a diff and every
# commit normalises it back — which destroys the one property the corpus needs:
# re-promoting an unchanged capture must produce no diff at all.
apps/mercado-livre/lib/marketplace/fixtures/__wire__/
