FIREBASE_PROJECT_ID=
FIREBASE_SERVICE_ACCOUNT=

# Firestore database ID. Firebase Enterprise uses `default` (no parens);
# Firebase free tier uses `(default)`. Leave blank to fall back to `default`.
FIREBASE_DATABASE_ID=default

WEBHOOK_TEST_SECRET=
