{
  "name": "@delfrance/functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "echo 'no lint configured for this package yet'",
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "test": "vitest run --config vitest.config.ts",
    "test:storage": "vitest run --config vitest.storage.config.ts",
    "clean": "rm -rf dist .turbo *.tsbuildinfo coverage"
  },
  "//runtime-deps": "@delfrance/auth + @delfrance/data + @delfrance/schemas are BUILD-time only: esbuild bundles them into the deploy artifact (only firebase-admin/firebase-functions/sharp stay `external`), so they live in devDependencies. The deploy does NOT upload this file — scripts/prepare-deploy.mjs emits a MINIMAL package.json (just the 3 `dependencies` below, no devDeps) into the generated .deploy/ folder, because the cloud `npm install` cannot resolve pnpm `workspace:*` (and the buildpack installs devDeps too). See apps/functions/DEPLOY.md.",
  "dependencies": {
    "@google-cloud/firestore": "^8.6.0",
    "firebase-admin": "^14.0.0",
    "firebase-functions": "^7.0.0",
    "sharp": "^0.34.0"
  },
  "devDependencies": {
    "@delfrance/auth": "workspace:*",
    "@delfrance/config-tsconfig": "workspace:*",
    "@delfrance/data": "workspace:*",
    "@delfrance/schemas": "workspace:*",
    "@types/node": "^24.10.0",
    "esbuild": "^0.25.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
