{
  "name": "@delfrance/mercado-pago-functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "//": "NOT a pnpm workspace package — apps/mercado-pago/functions is a deploy-artifact-only sub-build of @delfrance/mercado-pago-app (typecheck/lint/test run via apps/mercado-pago's tasks; tsconfig `**/*.ts` + `eslint .` + vitest `functions/**` cover it). This file is the TEMPLATE that scripts/prepare-deploy.mjs reads to emit the MINIMAL .deploy/mercado-pago-functions/package.json — only the 2 runtime deps below (no workspace:* — esbuild bundles @delfrance/*). Its deps must also be present in apps/mercado-pago's package.json so the bundle's externals resolve locally.",
  "dependencies": {
    "firebase-admin": "^14.0.0",
    "firebase-functions": "^7.3.0"
  }
}
