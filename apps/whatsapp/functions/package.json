{
  "name": "@delfrance/whatsapp-functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "//": "NOT a pnpm workspace package — apps/whatsapp/functions is a deploy-artifact-only sub-build of @delfrance/whatsapp-app (typecheck/lint/test run via apps/whatsapp's tasks; tsconfig `**/*.ts` + `eslint .` + vitest `functions/**` cover it). This file is the TEMPLATE that scripts/prepare-deploy.mjs reads to emit the MINIMAL .deploy/whatsapp-functions/package.json — only the 2 runtime deps below (no workspace:* — esbuild bundles @delfrance/*). Its deps must also be present in apps/whatsapp's package.json so the bundle's externals resolve locally.",
  "dependencies": {
    "firebase-admin": "^14.0.0",
    "firebase-functions": "^7.3.0"
  }
}
